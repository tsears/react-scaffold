import * as React from 'react'
import * as styles from './Header.m.css'

export const Header: React.StatelessComponent<{}> = (): React.ReactElement => (
  <div className={styles.header}>React Scaffold</div>
)
